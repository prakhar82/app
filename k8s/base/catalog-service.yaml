apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: grocery
spec:
  replicas: 1
  selector:
    matchLabels: {app: catalog-service}
  template:
    metadata:
      labels: {app: catalog-service}
    spec:
      containers:
        - name: app
          image: your-acr.azurecr.io/catalog-service:latest
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/catalog_db
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef: {name: grocery-secrets, key: SPRING_DATASOURCE_USERNAME}
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef: {name: grocery-secrets, key: SPRING_DATASOURCE_PASSWORD}
          readinessProbe:
            httpGet: {path: /actuator/health, port: 8082}
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /actuator/health, port: 8082}
            initialDelaySeconds: 40
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: grocery
spec:
  selector: {app: catalog-service}
  ports:
    - port: 8082
      targetPort: 8082
